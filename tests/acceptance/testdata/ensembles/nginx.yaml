# Note: keep allocations's resources equal as the assertions
# are taking that into account
version: "V1"
nginx_wrapper: &nginx_service
  type: service
  executor: docker
  resources:
    cpu:
      cores: 1
    gpus: []
    ram:
      size: 1
    disk:
      size: 1
  execution:
    type: docker
    image: nginxdemos/hello:plain-text
    working_directory: /
  keys: []
  provision: []
  healthcheck:
    type: command
    exec: ["nginx", "-t"]
    response:
      type: string
      value: "nginx: the configuration file /etc/nginx/nginx.conf syntax is ok"
allocations:
  nginx-node-1: *nginx_service
nodes:
  node1:
    allocations:
      - nginx-node-1
    ports:
      - private: 80
        public: 50001
        allocation: nginx-node-1
subnet:
  join: true
