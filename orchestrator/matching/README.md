# Subpackage `maching`

This is the orchestrator subpackage that will be holding the logic of matching required capabilities with machine available capabilities. This functionality is central for the decentralized computing model and we attempt to do it as open as possible for user-level configuration as well as further development.